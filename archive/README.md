# Archive — superseded material

Nothing in this folder is used by the pipeline. It is kept for history only.

| Folder | What it is |
|---|---|
| `poster-scripts/` | One-off scripts that generated the figures for the 2026 SPIN poster (`poster_figs.py`) and inserted them into the PowerPoint file (`build_poster.py`). They read a finished run's `master_everything.csv`; they are **not** part of the pipeline. |

Superseded **documentation** lives separately in [`../docs/archive/`](../docs/archive/).

> If you are looking for current documentation, start at
> [`../docs/Xon_Pipeline_Documentation.html`](../docs/Xon_Pipeline_Documentation.html)
> or the [`../docs/`](../docs/) folder.
